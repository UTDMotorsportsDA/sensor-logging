package com.company;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
//        // IP and port of the server
//        String serverName = "127.0.0.1";
//        int port = 7;
//
//        // all sensors
//        Sensor[] sensors = {new SpoofSensor("1"), new SpoofSensor("2"), new SpoofSensor("3")};
//
//        // client to collect and transmit data, server to receive data
//        DataLoggerClient client = new DataLoggerClient(serverName, port, sensors);
//        DataLoggerServer server = new DataLoggerServer(port);
//
//        // start each on separate thread to act like separate devices on network
//        new Thread(server).start();
//        new Thread(client).start();
//
//        // wait for some user input before ending
//        new Scanner(System.in).next();
//
//        client.end();
//        server.end();
    }
}
